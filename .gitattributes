# Normalise line endings to LF in the repository; editors on Windows can
# still check out CRLF if configured, but nothing is committed that way.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary
